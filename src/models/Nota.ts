//Creamos un modelo de nota
export class Nota {
    constructor(id: number, titulo: string, contenido: string, fecha: string) {
        id = id;
        titulo = titulo;
        contenido = contenido;
        fecha = fecha;
    }
}
//Creamos un modelo de usuario